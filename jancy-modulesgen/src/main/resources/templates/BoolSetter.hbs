{{{ javadoc }}}{{# if isDeprecated }}
    @Deprecated{{/ if }}
    public {{ className }} {{ name }}(boolean {{ name }}) {
        return new {{ className }}(withArgument("{{ originalName }}", {{ name }} ? "{{ trueValue }}" : "{{ falseValue }}"));
    }