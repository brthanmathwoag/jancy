{{{ javadoc }}}{{# if isDeprecated }}
    @Deprecated{{/ if }}
    public {{ className }} {{ name }}({{ typeName }} {{ name }}) {
        return new {{ className }}(withArgument("{{ originalName }}", {{ name }}.getValue()));
    }