{{{ javadoc }}}{{# if isDeprecated }}
    @Deprecated{{/ if }}
    public {{ className }} {{ name }}(String {{ name }}) {
        return new {{ className }}(withArgument("{{ originalName }}", {{ name }}));
    }