package {{ namespace }};

import jancy.core.Task;

/**
 * {{ javadoc }}
 */
public class {{ name }} extends jancy.core.Task {

    public {{ name }}(String name) {
        super(name);
    }

    protected {{ name }}(Task.State state) {
        super(state);
    }

    public String getModuleName() {
        return "{{ originalName }}";
    }

    {{# each options }}
    /**
     * {{ description }}
     */
    public {{ ../name }} {{ name }}(String {{ name }}) {
        return new {{ ../name }}(withActionArgument("{{ originalName }}", {{ name }}));
    }
    {{/ each}}

    {{# each modifiers }}
    public {{ ../name }} {{name}}(String {{ name }}) {
        return new {{ ../name }}(withModifier("{{ originalName }}", {{ name }}));
    }
    {{/ each}}
}
