package {{ namespace }};

import java.util.Map;

import jancy.core.Action;

{{ javadoc }}
public class {{ name }} extends jancy.core.Action {

    public {{ name }}() {
        super();
    }

    protected {{ name }}(Map<String, String> arguments) {
        super(arguments);
    }

    public String getModuleName() {
        return "{{ originalName }}";
    }

    {{# each options }}
{{ javadoc }}
    public {{ ../name }} {{ name }}(String {{ name }}) {
        return new {{ ../name }}(withArgument("{{ originalName }}", {{ name }}));
    }
    {{/ each }}
}
